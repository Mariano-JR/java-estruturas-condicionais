public class Composta {
    public static void main(String[] args) throws Exception {
        // Quando nossa condiciona utiliza de mais de um caminho, ou seja, que seja executado algo caso a condição passada seja False, utilizaremos a condicional composta, onde posterior ao If, utilizaremos o Else;
        // Em Java, tanto no If quanto no Else, se for passado mais de uma linha instrução, precisa da utilização de "{ }", caso seja apenas uma linha, não tem essa necessidade.
    }
}
